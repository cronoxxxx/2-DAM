#Selecciona las primera columna de la matriz `p` proporcionada, y guárdala en la variable `p1`.
# Sustituye los elementos de p1 que sean iguales a 0, por el valor $7$.
from operator import index, indexOf

import numpy as np
p = np.array([[1, 2, 4],
              [0, 0, 0],
              [0, 6, 8]])
#columna
p1 = p[:, 0]
p1[p1==0] = 7
print(p1)
print("Matriz")
print(p)